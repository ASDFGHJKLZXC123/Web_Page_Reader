GEMINI_API_KEY=your_key_here
GEMINI_MODEL=gemini-2.5-flash
GEMINI_EMBEDDING_MODEL=gemini-embedding-001
HOST=127.0.0.1
PORT=8787

# Optional: absolute path where backend stores notes (db.json + embeddings.ndjson).
# Defaults to backend/data/. The directory is auto-created if missing.
# NOTES_DIR=/absolute/path/to/notes

